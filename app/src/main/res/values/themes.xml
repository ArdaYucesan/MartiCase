<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MartiCase" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/marti_primary</item>
        <item name="colorPrimaryVariant">@color/marti_primary</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MartiCase" parent="Base.Theme.MartiCase" />
</resources>